var inv_perc = c3.generate({
  bindto: '#sales-gauge',
  data: {
    columns: [
      ['inventory % sold', <%= inventory_percent %>]
    ],
    type: 'gauge'
  },
  gauge: {
    label: {
      show: false
    },
    min: 0,
    max: 1
  },
  color: {
    pattern: ['#F97600', '#F6C600', '#60B044', '#F6C600', '#F97600'], // the three color levels for the percentage values.
    threshold: {
      values: [0.05, 0.15, 0.85, 0.95, 1.0]
    }
  },
  size: {
    height: 180,
    width: 360
  },
  legend: {
    hide: true
  },
  tooltip: {
    show: false
  }
});
